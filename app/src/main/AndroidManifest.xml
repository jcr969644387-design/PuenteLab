<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        PuenteLab es 100% offline. No se declara el permiso INTERNET
        ni ningún permiso relacionado con datos personales (contactos,
        ubicación, teléfono). No se usan cámara ni micrófono porque
        el proyecto no los requiere.
    -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.PuenteLab"
        android:hardwareAccelerated="true"
        android:name=".PuenteLabApp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PuenteLab"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CrashReportActivity"
            android:exported="false"
            android:process=":crashreport"
            android:theme="@style/Theme.PuenteLab"
            android:screenOrientation="portrait" />
    </application>

</manifest>
